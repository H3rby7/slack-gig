service: slack-orbo

frameworkVersion: ">=1.1.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs6.10
  region: eu-west-2

functions:
  create:
    handler: src/handler.slack
    events:
    - http:
        path: ''
        method: post
        cors: true

plugins:
- serverless-offline